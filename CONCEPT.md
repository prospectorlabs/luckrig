# luckrig — プロダクトコンセプト（草稿 v5）

---

## 一言で言うと

「今この瞬間、誰かのオンプレミス環境で動いているLLM推論APIを、貢献ベースで試せる場所」

---

## プロダクト名

**luckrig**

luck（引き当てる）+ rig（自分の環境・装備）。「ラッキーなリグを引き当てる」「自分のリグが誰かのラッキーになる」の両方向に読める。SLAを保証しないという立て付けとも整合している——ラッキーなら速いノードに当たる、ラッキーなら今動いている。Craigslistと同様に、かっこよさより実用性で選ばれることを目指す。

---

## カテゴリ定義

**オンプレミスLLM推論APIシェアリングプラットフォーム**

- オンプレミス：クラウドではなく、個人のマシンで動いている
- LLM推論API：OpenAI互換APIとして喋るテキスト生成に特化
- シェアリング：貢献ベースの共助
- プラットフォーム：トラッカーがコミュニティを束ねる

この組み合わせは新しい——AI HordeやPetalsという先行事例はあるが、いずれもluckrigとは差別化できる（詳細は §競合定義 を参照）。

LMSys Arenaがモデル評価のマップであるなら、luckrigはインフラ評価のリアルタイムマップである。モデルの優劣ではなく、特定の構成・環境・チューニングが今この瞬間どう動いているかを可観測にする。

---

## スコープの定義

v1はテキスト生成（OpenAI互換API）に限定する。

OpenAI互換のリクエスト/レスポンスを基線とし、ノード側プロキシ層に最小限の責務（token検証、ティア制御、ログ非書き出しの規約）だけを置く。subtext方式（後述）は基線の上に乗る任意の追加レイヤーとして提供し、必須にはしない。

ComfyUI（画像生成）、RVC（音声合成）、Whisper（音声認識）などは、subtext方式との整合性以前に試食UX（キュー/リプレイ/貢献スコア）の設計が別途必要なため、スコープ外とする。

射程が広いことは認識しているが、まず一つのプロトコルで設計を完成させる。

---

## 問題意識

AIの恩恵を受けるには、大手プラットフォームに金を払うか、高性能なGPUを自前で用意するしかない。この二択に収束している。

しかしローカルLLMの世界には、夜間に遊ばせているRTX 3080があり、独自にチューニングされた構成があり、誰も知らないノウハウが個人の手元に眠っている。

既存サービスはどれも「今すぐ、無料で、**特定の構成・環境・チューニングを指名して**、自分のプロンプトで試す」を満たせていない。

- HuggingFace Spaces：有料GPUかスリープ状態のCPUのみ。自分の環境の参考にならない
- CivitAI：重みファイルの展示会。触れない
- LMSys Arena：モデルも環境も選べない
- Vast.ai / SaladCloud：金銭が前提の商業モデル
- AI Horde（旧Stable Horde / KoboldAI Horde）：貢献ベース（kudos）でテキスト/画像生成APIを共助する直系の先行事例。ただし**どのワーカーに当たるかを抽象化する設計**で、ハードウェアや構成は隠される。luckrigは逆に**ハードウェアを主役にする**（環境メタデータ・チューニングノート・特定リグの指名）。
- Petals：大規模モデルを複数ノードで分散推論する共助プロジェクト。luckrigは1ノード1推論であり、分散推論は射程外。

この差別化が成立するのは、luckrigが「再現可能な構成情報」を一級のデータとして扱うことを前提にしているからである。「ラッキーなリグに当たる」ことを楽しみつつ、当たったリグの構成が再現可能な情報として手元に残る、というのが本プロダクトの核である。

---

## ターゲット定義

**ローカルLLMの構築に自力で立ち向かえるエンジニア。**

一般ユーザーは対象外。これは排他的な設計判断ではなく、積極的な設計判断である。

ターゲットを絞ることで以下が同時に解決する。

プライバシー非対称——ノード提供者は他人のプロンプトと出力を原理的に見える位置にいる。対象者が技術リテラシーの高いエンジニアであれば、リスクの自己評価ができる人だけが流入する。免責の根拠が一段強くなる。

虚偽リスティングの動機消去——物理法則を超える申告はコミュニティに即座に見抜かれる。コミュニティの目線が検証機能を兼ねる。

コミュニティの密度——ノード提供者と利用者の重複率が高くなり、貢献ベースの循環が回りやすい。Hotlineが機能したのも参加者の技術リテラシーが高かったからで、この構造的類似は意図して再現する。

設計の一貫性——「Hotline Connectの思想を移植」「ハッカー気質のアーリーアダプターを引き寄せる」「貢献ベースのアクセス権」が一直線に繋がる。

---

## コンセプトの原型

2000年代初頭、Hotline Connectというソフトウェアがあった。Mac向けのP2Pコミュニティツールで、ファイル共有・チャット・掲示板が一体化していた。サーバーはIPとポート番号だけでアドレスされ、一定の貢献をするとフルアクセス権のアカウントが払い出された。ADSLを引いたアーリーアダプターが自然とサーバー運営者になり、新着ファイルを提供することでプラットフォームの鮮度が保たれた。Botが転送レートを個人別に監視し、ダウンロード偏重のユーザーをキックした。

金銭ではなく貢献がアクセス権になる構造で、コミュニティが自律的に動いていた。

luckrigは、Hotlineの設計思想をオンプレミスLLM推論APIの共助に移植する試みである。

---

## 設計の骨格

### トラッカー

中央に軽量なトラッカーを置く。役割は三つ——ノードのエンドポイント登録の受付、定期的な死活監視、貢献スコアに応じた一時トークンの発行。

トラッカーはコンテンツを持たない。インフラを持たない。リストと権限だけを管理する。

### ノード

オンプレミス環境を持つ個人が自分のマシンをノードとして登録する。llama.cppやollamaがすでにOpenAI互換APIを喋るため、追加実装は原則不要。登録はエンドポイントURLを送るだけ。

ノードが落ちてもペナルティはない。トラッカーが自動でアベイラブルリストから外すだけ。

### アクセス権の三段階

無登録者はリストの閲覧まで。どんな環境でどんなモデルが稼働しているかが見える。ウィンドウショッピングで興味を持たせる。

無貢献登録者は制限付きアクセス。しょぼいモデル、厳しいクオータ、または出力の冒頭のみ。「もう少し使いたい」という欲求不満を設計的に作る。

貢献登録者は原則全モデルへのアクセスが可能。

---

## ノード登録情報の設計

```
モデル名    : Qwen3-35B-A3B
量子化      : Q4_K_XL
LoRA       : なし / あり（種類）
GPU        : RTX 5090 / 32GB VRAM
コンテキスト: 65536
tok/s      : 267（利用者側リプレイデータから算出）
稼働時間    : 平日夜〜朝
チューニングノート: （自由記述）
```

チューニングノートの例：

```
MTP n_max=2が最速。3以上は逆効果（accept率崩壊）。
長文RAGはSPEC=none推奨（prefill 3.4倍高速）。
n-gramはコード編集専用、創作・要約では逆効果。
ctx=131072がVRAM 32GBに収まる限界。
```

このノートがコミュニティに蓄積されていく。誰かのブログではなく、今動いているノードに紐づいた生きたノウハウとして。

---

## 貢献スコアの設計

### 基本方針

「使われた回数」だけを価値にすると、人気モデルだけが生き残りリストが収束する。多様性の維持がコミュニティの価値であるため、複数軸でスコアを設計する。

### スコアの構成

存在スコア——リスト登録継続時間（基礎点、小さめ）。

希少性スコア——同じGPU×モデル×量子化の組み合わせが少ないほど高い。ブルーオーシャンに飛び込むモチベーションが設計に織り込まれる。ラズパイノードは希少性スコアが最大になる。

使用スコア——実際のAPI呼び出し回数。

発見スコア——そのノードで「初めて試した」ユーザーの数。新しい組み合わせを開拓した先駆者が報われる。

ノートスコア——チューニングノートの参照回数 × 参照後のノード選択率。

### ゲーム化への耐性

事後評価を基本とする。「繋ぎっぱなしだが誰も使わないノード」はスコアが伸びない。「使われる」「参照される」ことが評価の本体。

### スコアモデル

ステータスモデルを採用する（一定スコアを超えたらフルアクセス権が永続的に得られる）。貢献スコアが転売可能な資産にならない設計を維持する限り、偽装してまでスコアを稼ぐ意味がない。

権利体系（永続権）とゲーム体系（希少性スコア・Showcaseランキング）は分離して設計する。永続権はHotline的な「貢献者への約束」として機能し、新規参入者のモチベーションは希少性スコアと発見スコアの別レイヤーで維持する。

---

## 貢献の形とグラデーション

- ノード提供（GPU + エンドポイント）→ 最大貢献
- チューニングノートの投稿 → 中程度
- 生成結果の公開 → 軽い貢献
- プロンプト・ワークフローの共有 → 軽い貢献
- 使うだけ（生成結果が自動公開される）→ 最小貢献

---

## 試食が購買に直結する設計——二方向の価値

### 方向1：自宅で再現可能な構成を確認する

「RTX 3080、Q4_K_Mで試して良かった」は、同じ構成を持つユーザーにとってそのまま再現可能な情報になる。自分の環境スペックを入力すれば近いノードをフィルタリングできる。

試食 → チューニングノートを読む → 自分でも再現できる確信が生まれる → インストールに踏み切る。

### 方向2：買えないマシンで投資判断をする

RTX 5090で実際にどれくらいのtok/sが出るかを自分のプロンプトで試せることは、雑誌レビューや他人のブログを100本読むより価値が高い。「次のボーナスで買うかどうか」を試食で決められる。

これは上位GPU提供者にとっての提供メリットでもある。自分のリグが他人の購買判断に貢献するという満足。

---

## インフラ評価のリアルタイムマップとして

トラッカーが死活監視する過程で、メモリ使用量、エラー率が自動的に蓄積される。

tok/sとTTFTの集約には、リプレイ側のタイミングメタデータを**利用者が明示的にopt-inでアップロード**する経路を用意する（プロンプト/レスポンス本文は送らない、タイミング数値だけ）。プライバシー（local-first）と看板（インフラ評価のリアルタイムマップ）の両立はこのopt-inで成立させる。

**計測の信頼性の置き方**

- plain mode（基線）では、レスポンスはOpenAI互換のSSEで真にストリームされる。chunk_timestampsは利用者端末で計測した実生成速度に対応する。
- subtext mode（任意の追加レイヤー）では、ノード側プロキシがレスポンスを完全バッファしてから擬似SSEで返すため、利用者側のchunk_timestampsは「ノードプロキシが再生したレート」であって実生成速度ではない。この場合のtok/sはノードプロキシが計測した値（response envelope内）を一次ソースとして扱い、利用者側はそれをそのままリプレイに永続化する。

いずれのモードでも、tok/sを「ノード自身がリスティングに書いた自己申告値」ではなく**計測経路に紐づいた実測値**で扱う原則は維持する。

稼働データから自動生成されるノウハウの例：
- RTX 4070TiならQ4_K_Mが実用限界
- このLoRAはQ8_0の方が効く
- M3 MaxはVRAM統合だからこのモデルが意外と速い
- ラズパイ5でllama3.2-1B / Q4_K_Mは2.3 tok/sで動く

### 低スペックリスティングをShowcaseとして扱う

トラッカー側で自動生成するShowcaseカテゴリ：
- 最低スペックで動いているノード
- 最も古いGPUノード
- 最も低消費電力で動いているノード

順位ではなく多様性の見える化。Leaderboardではなく、Showcase。

新規利用者の試食UIで、デフォルトを上位スペック順ではなく希少性スコア順に並べる。思想をUIで表現する。

## リプレイ機能——手元への永続化

キューに並ぶ設計上、ユーザーは待ち時間に席を離れる可能性がある。擬似SSEで流れてくるレスポンスをリアルタイムで視認できないケースが生じる。リプレイ機能がこれを解決する。

バッファ段階で以下の情報が手元に揃っている。追加の計測コストはゼロ。

```json
{
  "schema_version": 1,
  "prompt": "実際のプロンプト（平文）",
  "response": "実際のレスポンス（復号済み平文）",
  "node_id": "xxx",
  "queue_wait_sec": 12,
  "generation_sec": 8.3,
  "tok_per_sec": 267,
  "ttft_ms": 842,
  "chunk_timestamps": [...]
}
```

`tok_per_sec`と`ttft_ms`の一次ソースはモードによって異なる（§インフラ評価のリアルタイムマップを参照）。plain modeでは利用者側のchunk_timestampsが実生成速度に対応し、subtext modeではノードプロキシ計測値（response envelope内）が一次ソースとなる。いずれのケースも、ノードが自分で「リスティングに掲げた自己申告値」を一次ソースにはしない。

リプレイ本体（プロンプト/レスポンス本文を含む）はローカルに永続化する。サーバーには送らない。

**opt-inのタイミングメタデータ共有**

「インフラ評価のリアルタイムマップ」を成立させるため、利用者は試食ごとに**タイミングメタデータのみ**をトラッカーへopt-inでアップロードできる（`tok_per_sec` / `ttft_ms` / `generation_sec` / `queue_wait_sec` / `output_tokens` / `tokenizer` / `created_at` / `mode`）。**プロンプトとレスポンス本文は決して送らない**。アップロードは試食UIで明示的なボタンを押した場合のみ発生し、デフォルトはオフ。

このopt-inがないと、看板である「インフラ評価のリアルタイムマップ」は最も知りたい数値（tok/s）を表示できない。local-first-by-defaultを保ったまま、ユーザーが自分の意思で集合知に貢献できる導線として位置付ける。

```
~/.luckrig/history/
  2026-05-22_143022_{node_id}.json
```

`schema_version`を持つことで、後から自動削除・サイズ上限・簡易モードなどの選択肢を足せる余地を残す。

リプレイ時はchunk_timestampsを使って当時の生成速度を再現する。ユーザーは「キュー待ち12秒、生成267tok/s、TTFT 842ms」という体験をそのまま追体験できる。

**リプレイのTTFTはリアルタイム体験より正確**

リアルタイムSSEでのTTFT体感には、ユーザー端末からノードまでのネットワークレイテンシが混入する。場所・ISP・ノードへの経路によって100ms〜500msの揺れが生じるため、インフラ評価として純粋ではない。

一方リプレイに記録されるTTFTは、ノード側プロキシがSSEの最初のチャンク到達を記録した値——推論エンジン単体の性能に近い数値である。ネットワーク変数を排除したことで、「このGPU・このモデル・このチューニングでのTTFT」をより正確に評価できる。

plain modeでは、TTFTは利用者端末側の真のSSE体感値となる（ネットワーク変数を含む）。subtext modeを使う場合は、ノードプロキシがSSEの最初のチャンク到達を記録した値が一次ソースとなり、ネットワーク変数を排除した純粋インフラ評価値を得られる。リプレイはどちらの値も保持する（`network_ttft_ms` と `proxy_ttft_ms`）。

リプレイは妥協の産物ではなく、luckrigが「インフラ評価のリアルタイムマップ」であるというコンセプトに忠実な設計である。副次的な価値として、リプレイデータが時系列でのノード評価にも使える。

---

## リスク対処

### プライバシー設計——「頑張らなければ見えない」

ノード提供者は推論を実行している以上、原理的にプロンプトと出力を見える位置にいる。これを完全に隠蔽することはできない。

luckrigは2つのモードを提供する。**plain mode（基線）**は、OpenAI互換APIをそのまま喋る。プライバシー保護はTLSによる経路秘匿と、ノードプロキシの「素の平文をログに書き出さない」規約のみに依存する。素のOpenAIクライアントが追加実装なしで動く。

**subtext mode（任意の追加レイヤー）**は、basecaseの上に「ノードのプロキシログにcovertextしか残らない」というdefense-in-depthを足す。subtextはAES-GCM暗号化済みペイロードをUnicode variation selectorとして埋め込む方式で、ノード提供者が雑にプロキシログをコピペしても平文が漏れない。引き換えに、レスポンスをノード側で全バッファしてから一括で隠蔽するため、SSEは擬似SSEになる（後述）。

この仕組み（subtext）は価値訴求の主役にしない。luckrigはセキュリティ製品ではなく、秘密を預ける場所でもない。subtextはあくまで「装飾的な気遣い」であり、その内訳もドキュメントで素直に開示する。

利用者向けの説明は、モードの差分よりも運用上の注意を前に出す——送信内容は通常ログやtcpdumpで簡単に読めないよう処理しているが、やる気のあるノード提供者がプロセス内部に踏み込めば原理的に見える。したがって機密情報、個人情報、業務上守るべき内容は送らない。この温度感をUIとドキュメントで一貫させる。

**通信フローの設計（subtext mode）**

```
利用申請時：
  利用者ID + 公開鍵 → トラッカー → ノードのプロキシ層にストア

プロンプト送信時：
  利用者がノードの公開鍵でプロンプトをAES-GCM暗号化
  → subtextでカバーテキスト（固定文字列）に隠蔽して送信

レスポンス受信時：
  ノードのプロキシ層がレスポンスを利用者の公開鍵で暗号化
  → subtextでカバーテキストに隠蔽して返送
  利用者が秘密鍵で復号
```

**subtextの仕組み**

Unicode variation selectorを使って暗号化済みペイロードをテキストに不可視で埋め込む方式。カバーテキストは固定文字列をハードコードして使用するため生成コストがゼロ。セキュリティはAES-GCM側が担保しており、カバーテキストの内容はセキュリティに無関係。通信経路上に流れるのはカバーテキストだけで、ノードのログに平文は残らない。

**隠蔽の射程と限界**

「頑張らなければ見えない」の射程を正確に定義する。

TLS外側で観測する第三者（経路上の第三者、tcpdump）には、カバーテキストしか見えない。ログにも平文は残らない。

ただしノード提供者がプロセス内側に踏み込む場合——メモリダンプ、llama.cppの入力フック、プロセスデバッグ——は止められない。推論エンジンは平文プロンプトを必要とするため、プロセス内側では必ず平文が存在する。

「頑張らなければ見えない」は「tcpdumpでは見えないが、プロセスに踏み込めば見える」という意味であり、この限界はエンジニア向けの免責と組み合わせることで設計上の前提として明示する。

**鍵配布のtrust model**

利用者がトラッカーでノードを選択して利用申請する際に、利用者IDと公開鍵をトラッカーに送信。トラッカーが該当ノードのプロキシ層に転送し、自動でストアされる。

trust modelを明示する——トラッカー単独では平文を読めない。ノード単独でも利用者の秘密鍵がないため復号できない。ただしトラッカー+ノードが共謀して公開鍵をすり替えた場合は、平文を読める構造になる。

「トラッカー運営者（luckrig）を信頼する必要があるか」という問いへの答えはYes。ただしluckrig単独では読めず、ノード提供者との共謀が必要という条件付き。これを利用者に明示することで、適切なリスク評価を促す。

任意機能として、ノードオーナーが別経路（GitHubなど）で公開鍵フィンガープリントを公表するパターンを許容する。エンジニア向けに自己検証の余地を残す。

公開鍵のストアはセッションスコープ。トークン失効と同時に破棄されるため、ノード側に利用者情報が蓄積されない。「頑張っても残っていない」。

**ノード側の実装——luckrigプロキシ層**

ノード登録コマンド一行で、ollama/llama.cppの前段にプロキシ層が自動で差し込まれる。

```
利用者 → [luckrigプロキシ層] → ollama/llama.cpp
               ↑
  subtext復号 / 暗号化&subtext注入
  ログには平文を吐かない
```

VibeProxyと構造的に同一。APIキー隠蔽の代わりにプロンプト・レスポンスの内容を隠蔽する層として機能する。

**SSEとの両立**

plain modeでは、ノードプロキシはupstreamのSSEチャンクをそのまま転送する。利用者には真のSSEが届き、tok/sもTTFTも端末で実測できる。OpenAI互換クライアントは追加実装なしで使える。

subtext modeでは、SSEによる逐次受信とsubtext隠蔽が原理的に相性が悪い。subtextは完成したペイロード全体を一塊として隠蔽する仕組みであるため、トークン単位で流れるSSEチャンクをそのまま隠蔽できない。これをUXレベルで解決する。

```
利用者がノードを選択
  ↓
キュー待ち表示（この間プロキシ層がSSEチャンクをバッファ）
  ↓
生成完了 → 一括でAES-GCM暗号化 → subtext埋め込み
  ↓
「接続しました」→ 擬似SSEで利用者に送信
```

GeForce Nowがゲーム機の空きを待たせるのと同じUXパターン。利用者はノードの利用権のキューに並んでいると認識するため、待ち時間を「生成が遅い」と解釈しない。キューを抜けた瞬間にレスポンスが流れてくる体験になる。キュー UX自体はplain modeでも適用される（ノードの`max_active`を超えるリクエストはどちらにせよ待たされる）。

キュー待ち人数の表示がプラットフォームの活況感を演出する効果もある。

**盗聴と改竄の非対称について**

subtext方式の導入により、盗聴コストは上がった——プロセスに踏み込まないとプロンプトが見えない。ただし改竄（出力のすり替え）の難易度は変わらない。プロキシ層を改造すれば実行可能なままである。「盗聴は難しくなったが改竄は同じ難易度」という非対称が残ることを設計上の前提として明示する。

### フィルタリング方針

luckrigは技術探求の遊び場であり、創作の遊び場ではない。

NSFWコンテンツは通さない。プロンプト段階での検知を基本とする。グレーゾーンの創作（暴力描写、心理的に重い創作、二次創作キャラクターのシミュレーション）についても、「luckrigはその場ではない、それをやりたいなら自分のローカル環境でやってほしい」という立場を最初から明示する。

免責とは別軸の宣言。コミュニティの性格を決めるもの。具体的な判定基準は実例が出てきてから整備する。

### 虚偽リスティングへの設計的対処

検証実装ではなく、動機の構造的消去を選ぶ。

スコア稼ぎ目的の偽装——ステータスモデル（フルアクセス権が転売不可）の採用により、偽装してまでスコアを稼ぐ意味がない。

注目集め目的の偽装——物理法則を超える申告はコミュニティに即座に見抜かれる。

悪意ある出力すり替え——免責前提・公開前提のプラットフォームでは流れてくるプロンプトの価値が低く、収集コストに見合わない。またtok/sを利用者側で計測するため、性能偽装もリプレイデータで露見する。

この判断により、Canary promptもattestationも実装しないことを積極的に選ぶ。「検証しないことを設計で正当化する」。

### ノード提供側の参入障壁とその解消

セキュリティ不安——Tailscaleのようなオーバーレイネットワーク経由で実IPを隠せる。

主権の喪失——GPU使用率の上限を設定できる。オンオフ自由。

運用継続プレッシャー——ペナルティなし。トラッカーが自動でリストから外すだけ。

導入コスト——ollamaが入っていればコマンド一行で登録完了。

動機の不明瞭さ——貢献スコアが増え、自分のノードが試食リストに載る。希少な構成なら希少性スコアで上位に来る。自分のチューニングが他人の購買判断に使われる。

### 免責の設計

「試食」というコンセプトが免責の根拠になる。

利用者向け：品質保証なし、可用性保証なし、セキュリティ保証なし（trust modelを理解した上で使うこと）、業務利用は自己責任。

ノード提供者向け：落ちても責任なし、出力の品質に責任なし、稼働時間の保証なし。

「保証します」より「保証しません、でも試せます」の方が、このコンテキストでは誠実であり、かつハッカー気質のアーリーアダプターを引き寄せる。実験的であること自体がブランドになる。

---

## 最小実装の順序

初期ノード提供者への価値を先に立ち上げるため、試食UIより公開リストを先に作る。

1. ノード情報の公開リストを作る（無登録者向け、死活監視込み）
2. ベンチマーク自動収集を実装する（メモリ使用量、エラー率の自動記録）
3. ノード登録CLIを作る（コマンド一行、luckrigプロキシ層の自動差し込み）
4. トークン発行ロジックを実装する
5. 試食UIを作る（トークン取得→キューUX→擬似SSE）
6. リプレイ機能を実装する（ローカル永続化）
7. 貢献スコアと権限管理を実装する

既存のllama.cpp環境（Qwen3.6-35B-A3B、RTX 5090、port 8088）がそのまま初号ノードになれる。

ただし初号ノードだけでは「結局5090がないと無理」という印象を与えかねない。初期フェーズで意図的にラズパイ、M3 Max、RTX 2080あたりのノードを揃える協力者を巻き込むことが、プラットフォームの最初の印象を決める。

---

## v6以降の宿題

フィルタリングの具体的な閾値設計と運用体制——コミュニティ規模が立ち上がってから設計する。

ステータスモデルの長期的な階層固定化——権利体系（永続権）とゲーム体系（希少性スコア・Showcaseランキング）を分離する設計方針は確定済み。具体的な再評価サイクルはv6以降。

テキスト生成以外のスコープ拡張——subtext方式との整合性設計が確立してから検討する。

継続的貢献の動機設計（消費財ループ）——AI Hordeのkudos的な「優先度を消費財として買う」閉じた経済を、Hotline的な永続権モデルと併設するかは未確定。永続権はそのままに、希少性スコア・Showcaseに加えて優先度キュー短縮を消費財として置くかどうかをv6で検討する。

「読むだけ層」の取り込み——チューニングノートと公開タイミングメタデータは、自分でリグを建てない層にも価値がある。「触れる人（エンジニア）」と「読むだけの人（読者）」のUI/権限を分離するかどうかをv6で検討する。

ノード提供者保護のためのフィルタ強化と通報導線、トラッカー運営者の法的立て付け——v1の必須スコープに繰り上げて整備する（§フィルタリング方針 / §免責の設計を参照）。

---

*最終更新：2026-05-22（v5.4、plain modeを基線に、subtextを任意追加レイヤーに整理／opt-inタイミング共有を追加／AI Horde・Petalsを競合に明記／継続動機・読むだけ層をv6宿題に追加）*
